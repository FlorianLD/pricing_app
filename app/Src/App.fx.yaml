App As appinfo:
    BackEnabled: =true
    Formulas: =Set(yPosition, 200)
    OnStart: |-
        =Set(FormAlignment, 35);
        Set(CurrentPage, "products_page");
        filterDD_Status.SelectedValue = "All";
        filterDD_Employee.SelectedValue = "All requests";
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

